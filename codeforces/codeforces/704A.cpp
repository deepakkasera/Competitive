#include<bits/stdc++.h>
using namespace std;
int main(){
	int n,q;
	cin>>n>>q;
	int x,y,a=0;
	vector< int > v[300050];
	set<int>  s;
	while(q--){
		cin>>x>>y;
		if(x==1)   a++,
		else if(x==2)   ans-=num[y],num[y]=0;
		else    
		    int i=
		    while(i<=y)
	}	
	return 0;
	
}
